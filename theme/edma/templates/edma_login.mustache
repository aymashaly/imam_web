{{> theme_boost/head }}

<body {{{ bodyattributes }}}>
{{> core/local/toast/wrapper}}

<div id="wrapper" class="edma-page-wrapper">

    {{> theme_boost/navbar }}

    {{{ output.standard_top_of_body_html }}}

    {{^ output.if_home_pages}}
        <div class="pages-banner-area ptb-100">
            <div class="container">
                <div class="pages-banner-content wow animate__animated animate__fadeInUp delay-0-2s">
                    <h2>{{{ pageheading }}}</h2>

                    {{{ output.navbar }}}
                </div>
            </div>

            {{#banner_shape_image}}
                <img src="{{banner_shape_image}}" class="shape shape-1" alt="{{sitename}}">
            {{/banner_shape_image}}
            
                {{#banner_shape_image2}}
                <img src="{{banner_shape_image2}}" class="shape shape-2" alt="{{sitename}}">
            {{/banner_shape_image2}}

                {{#banner_shape_image3}}
                <img src="{{banner_shape_image3}}" class="shape shape-3" alt="{{sitename}}">
            {{/banner_shape_image3}}
        </div>
    {{/output.if_home_pages}}

    <div class="login-area">
        <div class="container">
            <div id="page-content" class="row">
                <div id="region-main-box" class="col-12">
                    <section id="region-main" class="col-12 h-100" aria-label="{{#str}}content{{/str}}">
                    <div class="login-wrapper pt-100 pb-75">
                        <div class="login-container">
                        {{{ output.main_content }}}
                        </div>
                    </div>
                    </section>
                </div>
            </div>
        </div>
    </div>

    {{> theme_boost/footer }}
</div>

</body>
</html>
{{#js}}
  M.util.js_pending('theme_boost/loader');
  require(['theme_boost/loader'], function() {
  M.util.js_complete('theme_boost/loader');
  });
  M.util.js_pending('theme_boost/drawer');
  require(['theme_boost/drawer'], function(mod) {
  mod.init();
  M.util.js_complete('theme_boost/drawer');
  });
{{/js}}
