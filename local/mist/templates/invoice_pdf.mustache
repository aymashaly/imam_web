<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Invoice {{invoice.reference}}</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 10pt; }
        .invoice-container { width: 100%; max-width: 800px; margin: 0 auto; }
        .header { display: flex; justify-content: space-between; margin-bottom: 20px; }
        .company-info { flex: 1; }
        .invoice-info { text-align: right; }
        .invoice-meta { margin-top: 10px; }
        .content { margin: 30px 0; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f5f5f5; }
        .total-row { font-weight: bold; }
        .footer { margin-top: 50px; text-align: center; font-size: 9pt; color: #666; }
        .logo { max-height: 80px; max-width: 200px; margin-bottom: 10px; }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            <div class="company-info">
                {{#company.logo}}<img src="{{company.logo}}" alt="Company Logo" class="logo">{{/company.logo}}
                <h2>{{company.name}}</h2>
                <div>{{company.details}}</div>
            </div>
            <div class="invoice-info">
                <h1>INVOICE</h1>
                <div class="invoice-meta">
                    <p><strong>Reference:</strong> {{invoice.reference}}</p>
                    <p><strong>Date:</strong> {{dates.created}}</p>
                    <p><strong>Due Date:</strong> {{dates.due}}</p>
                </div>
            </div>
        </div>
        
        <div class="content">
            <div class="bill-to">
                <h3>Bill To:</h3>
                <p>{{user.firstname}} {{user.lastname}}<br>
                {{user.email}}</p>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Unit Price</th>
                        <th>Tax</th>
                        <th>Qty</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    {{{items_html}}}
                    <tr class="total-row">
                        <td colspan="4" style="text-align: right;">Subtotal:</td>
                        <td>{{currency}}{{invoice.amount}}</td>
                    </tr>
                    <tr class="total-row">
                        <td colspan="4" style="text-align: right;">Tax:</td>
                        <td>{{currency}}{{total_tax}}</td>
                    </tr>
                    <tr class="total-row">
                        <td colspan="4" style="text-align: right;">Total:</td>
                        <td>{{currency}}{{invoice.total}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <div class="footer">
            <p>{{payment_instructions}}</p>
            <p>Thank you for your business!</p>
        </div>
    </div>
</body>
</html>